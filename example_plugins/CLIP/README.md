# CLIP Plugin

Text-based image search using OpenAI CLIP. The model is downloaded automatically on first run.

## Setup

```sh
uv run main.py
```

Run this once before starting Luminous to ensure dependencies are installed and the model is cached. The daemon starts automatically when Luminous loads the plugin.
