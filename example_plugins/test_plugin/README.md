# Test plugin

Example plugin for decoding and encoding a single red pixel.

## Build
```sh
cd <this-folder>
mkdir build
cmake -S . -B build && cmake --build build && mv build/libtest_plugin.so .
```
